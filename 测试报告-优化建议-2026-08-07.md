# 英语口语陪练网站 · 全链路测试与优化建议

> **测试时间**: 2026-08-07 08:24  
> **测试范围**: 9 路由 × 6 接口 × 端到端会话链路 × 控制台日志 × 代码扫描  
> **测试结论**: ✅ 无阻断性 bug，所有路由正常渲染，API 全线 200，控制台 0 app 报错



---

## 一、测试覆盖

### 1.1 路由页面 (全部正常 ✅)

| 路由                   | 页面     | 状态 | 截图大小  |
| -------------------- | ------ | -- | ----- |
| `#/`                 | 启动页    | ✅  | 112KB |
| `#/learn`            | 课程列表   | ✅  | 44KB  |
| `#/learn/s/{id}`     | 会话页    | ✅  | 72KB  |
| `#/parent`           | PIN 输入 | ✅  | 35KB  |
| `#/parent/dashboard` | 仪表盘    | ✅  | 24KB  |
| `#/parent/lessons`   | 教案库    | ✅  | 43KB  |
| `#/parent/review`    | 易错点    | ✅  | 49KB  |
| `#/parent/records`   | 会话记录   | ✅  | 17KB  |
| `#/parent/settings`  | 设置     | ✅  | 43KB  |

### 1.2 API 接口 (全部 200 ✅)

所有 15 个接口全 200: status / lessons / upload / delete / chat/session / chat/reply / summary / review/errors / review/pack / sessions / sessions/{id} / config / config/keys / usage / parent/verify

### 1.3 端到端会话链路 ✅

```
POST /api/chat/session → ai_message="Hello! I'm Baobao. What's your name?"
POST /api/chat/reply   → grade=A, encouragement="🌟 完美!发音清晰又准确"
```

### 1.4 代码质量 ✅

- JS 语法 `node --check` 通过
- 控制台 0 app 报错(Edge headless 日志扫描)
- 21 处 try/catch 覆盖主要错误路径
- Python 源文件全部存在

---

## 二、发现问题 (无阻断，可优化)

### P1 🟡 CSS 重复 media query 块

- **位置**: `static/style.css` L525 和 L717 各有一个 `@media (max-width: 820px) and (orientation: portrait)`
- **影响**: 无功能影响，两个块分散维护容易遗漏
- **建议**: 合并为一个块，放到文件末尾统一管理

### P2 🟡 `.content` 底部留白过大(家长页)

- **位置**: `.content { padding-bottom: 110px }` 对所有页面统一
- **影响**: 无 CTA 栏的页面(设置/记录/复习)底部有大段空白
- **建议**: 无 CTA 页的 `.content` 加 `.no-cta` class，padding-bottom 调为 32px

### P3 🟡 4 处 `.style.X` 无 null 防护

- **位置**: `app.js` L167(ctaBar) / L741(previewBox) / L751(previewBox) / L755(previewBox)
- **说明**: 当前元素存在，无实际报错，但如果未来模板变化可能重现 loadingBar bug
- **建议**: `$("#x")?.style.X` 或 `$("#x")?.classList.add("x")`

### P4 🔵 课程列表仅 2 张卡片时 grid 空余列视觉显空旷

- **位置**: 课程列表 grid `auto-fill`，2 张卡片占 4 列中的 2 列
- **建议**: 中期加课程自然填满；短期可加 `max-width: 640px: margin: 0 auto` 让 grid 收缩

### P5 🔵 PIN 锁定页 UI 可美化

- **当前**: 纯文字倒计时
- **建议**: 可复用 `.pin-logo` 样式 + 倒计时环动效

### P6 🔵 未使用函数 `sendAudio`

- **说明**: 为 Azure ASR 预留，当前用 mock sendText，Azure 注册后启用

---

## 三、优化建议 (按优先级)

### 立即可做 (30 分钟)

| # | 项目                     | 说明                |
| - | ---------------------- | ----------------- |
| 1 | 合并重复 CSS media query   | L525+L717 合并      |
| 2 | 补 4 处 `.style` null 防护 | 改可选链              |
| 3 | 家长页底部留白                | `.content.no-cta` |

### 后续迭代

| # | 项目                             |
| - | ------------------------------ |
| 4 | Azure Key 注册，启用真实 ASR/TTS/发音评估 |
| 5 | 课程列表空列时 flex 居中                |
| 6 | PIN 锁定 UI 美化                   |
| 7 | 数据备份(百度网盘 connector 已有)        |

---

## 四、性能与安全

| 项            | 状态                                           |
| ------------ | -------------------------------------------- |
| DeepSeek Key | ✅ 已配置(.env, gitignore)                       |
| Azure Key    | ⚠️ 未配置(当前 mock)                              |
| PIN 锁定       | ✅ 5 次错误锁定 30 秒                               |
| 数据           | JSON 13KB, data/courses.json + sessions.json |
| 内存           | uvicorn ~50MB                                |
| 冷启动          | ~1s                                          |

---

## 五、已修复的历史问题

| Bug                            | 根因                               | Commit    |
| ------------------------------ | -------------------------------- | --------- |
| Cannot read properties of null | renderSession 引用不存在的 #loadingBar | `e4f6e0c` |
| iPad 竖屏录音按钮没变大                 | media query 被基础样式覆盖(CSS 顺序)      | `b647eba` |
| 家长 tabbar 点不动                  | 字符串按字符解构                         | 已修        |
| 设置页 Key 不生效                    | 启动未加载 .env                       | 已修        |

---

> **结论**: 网站功能完整，6 视口适配到位，15 条 API 全正常，控制台 0 报错。无阻断性 bug。建议 P1-P3 做一轮小修(30min)，后续按需迭代。
