# 英语口语陪练网站 · 网页版落地规划

> 版本:v3.0 · 2026-08-06 · 依据 2026-08-06 设计讨论 + 真实教案样本(Unit01/Unit02)重写
> 技术路线:**国际版 Azure.com(识别/合成/发音评估)+ DeepSeek(解析/对话/评估/记录/规划)**
> 目标用户:女儿(悦琳),小学三升四(约 9 岁),日常口语练习较少
> 状态:**规划阶段**——确认本规划后进入 P0 开发

---

## 0. 定位一句话

> **给悦琳的「开口就有反馈」的英语口语练习网页 + 给爸爸的「看得见进步」的管理后台。**

设计总原则(Apple 设计语言):
- 「简洁」不是藏功能,而是**主任务无摩擦、信息层级一眼可读**
- 通过顺序、间距、字重、对比建立清晰度,每个元素都有存在理由
- 材质只服务导航与控件,内容层克制;颜色只用于交互和状态,不做大面积装饰
- 列表比堆叠卡片更适合可扫描信息;主操作每屏只突出一个

**核心差异化(为什么不用豆包)**:教案锚点固化进 DeepSeek 系统提示词(不占对话上下文)、跟读记录由系统自动结构化持久化——彻底解决豆包长对话上下文丢失、记录不全的问题。

---

## 1. 目标用户与场景

| 项 | 结论 |
|---|---|
| 学员 | 悦琳,小学三升四(约 9 岁),口语练习较少、基础薄弱 |
| 教练 | AI(按教案"带读规则"扮演教师,Teacher 角色) |
| 爸爸 | 家长:上传教案、与 AI 沟通调整、查看记录与进步 |
| 使用设备 | 电脑为主(先本地 + 局域网) |
| 单次时长 | 25~28 分钟三段式(教案规定),或孩子喊停即止 |
| 教案来源 | 爸爸每次练习前准备的 TXT/DOCX(已有 Unit01/02 真实样本) |

**设计决策(由用户特点推导)**:
- 操作引导用中文(按钮/提示),练习内容用英文(AI 语音与字幕)
- 反馈鼓励式:先肯定再建议;连续两次未完成 → 示范一次完整跟读,不施压
- 语音点/关键句型/过关标准全部来自教案,AI 不自由发挥
- 轻量激励:每环节结束记录 A/B/C/D + 一句鼓励,不搞复杂成就系统

---

## 2. 技术方案

### 2.1 能力分工

| 能力 | 服务 | 说明 |
|---|---|---|
| ASR 语音识别 | Azure 语音(国际版) | 识别悦琳说的英语 |
| 发音评估 | Azure 发音评估(free-form) | **核心能力**:逐词打分;经典版含在识别额度内 |
| TTS 语音合成 | Azure 神经语音 | 英语自然,首包延迟约 120ms |
| 教案解析 | DeepSeek | 上传的 TXT/DOCX → 结构化教案 |
| 对话驱动 | DeepSeek | 按教案锚点扮演教师、控制节奏 |
| 评估记录 | DeepSeek | 每环节 A/B/C/D、真实语句摘录、易错点总结 |
| 复习规划 | DeepSeek | 薄弱项 + 第 1/3/7 天回测建议 |

### 2.2 Azure 国际版要点

| 项 | 说明 |
|---|---|
| 注册 | 邮箱 + 手机 + 非预付外币卡;$1 验证费可退 |
| 免费额度 | $200/30 天 + F0 永久免费层(识别 5 小时/月 + 合成 50 万字符/月) |
| 服务区域 | eastasia / japaneast(国内访问更稳定);账单地址可选中国 |
| 发音评估 | `PronunciationAssessmentConfig(reference_text="")` free-form 模式 |
| 月成本 | 约 ¥15~20(超免费部分)+ DeepSeek ¥2~5 ≈ ¥20 |

### 2.3 系统架构(简要)

```
浏览器(录音 / 播放 / 逐词展示)
   │  HTTP / WebSocket
FastAPI 后端(API Key 只存后端 .env)
   ├─ 教案导入:解析 TXT/DOCX → 结构化课程
   ├─ Azure ASR / 发音评估 / TTS
   ├─ DeepSeek:教案解析 / 教师对话 / 评估记录 / 复习规划
   ├─ 课程与记录存储(本地 JSON/SQLite,课程 + 会话 + 跟读记录)
   └─ 用量统计(每月重置,防超免费额度)
```

- 无真实 Key 时全链路 mock 可跑 → 先开发后联调
- 语音链路:录音(静音检测)→ ASR → 发音评估 → DeepSeek 判断 → TTS 播放

---

## 3. 核心业务闭环(教案 → 练习 → 记录 → 复习)

这是整个系统的灵魂,直接映射爸爸现有的工作方式,并修复豆包的缺陷:

```
① 爸爸准备教案(TXT/DOCX/PDF/图片,如 Unit01/02)
        ↓ 家长模式 · 上传
② DeepSeek 解析教案 → 结构化课程
   单元/环节/核心词/句型/必做对话/语音点/过关标准/带读规则/A-B-C-D 记录规则
        ↓
③ 学习模式 · 选择课程 → 按教案分段练习(每段 6~8 分钟)
   AI 教师按带读规则提问 → 悦琳开口 → Azure 识别+逐词发音反馈
   每段结束:AI 明确"这一段完成",自动判定并记录 A/B/C/D
        ↓
④ 教案完成 → 若家长已确认复习包,自动追加「REVIEW 复习环节」
   逐条重练往期易错句(家长勾选内容)→ 完成后结束会话
        ↓
⑤ 会话结束 → DeepSeek 生成结构化跟读记录(自动持久化,不丢上下文)
   分段 A/B/C/D 结果 + 真实语句摘录 + 说错句子(weak_lines)+ 薄弱项
        ↓
⑥ 家长模式 · 易错点复习
   从历史记录聚合"说错的句子 + 易错点总结" → 家长勾选(≤6 条)→ 确认保存复习包
   → 下节课自动追加 REVIEW 环节(教学内容以教案为主,复习为辅,复习内容必须家长确认)
```

**对比豆包**:豆包靠对话记忆,长对话截断 → 记录不全;本系统教案锚点在提示词、记录落库,任何时刻可回看、不丢失。易错点复习由家长把关,AI 不自动给学生加练。

---

## 4. 双模式设计(情景区分)

| 维度 | 学习模式(悦琳) | 家长模式(爸爸) |
|---|---|---|
| 进入 | 启动页大按钮,一键直达、零验证 | 小入口 + PIN(4 位)验证 |
| 语言 | 练习内容英文 + 操作引导中文 | 全中文 |
| 信息形态 | 图形化:课程卡片、会话气泡、逐词着色 | 数据化:列表、图表、摘要 |
| 节奏 | 对话式、短反馈、鼓励性 | 扫读式、可对比 |
| 主操作 | 每屏一个:大说话按钮 | 每屏一个:上传 / 查看 / 配置 |
| 视觉 | 明亮、友好、大字号(整体放大一档) | 克制、专业、信息密度高 |
| 内容边界 | **永不出现家长内容**(无上传/统计/设置) | 可见全部学习数据 |

导航完全独立,只在启动页交汇。

---

## 5. 信息架构

```
启动页(模式选择)
├─ 学习模式(悦琳)
│   ├─ 课程选择        —— 课程卡片(Unit 01 见面问候 / Unit 02 数字年龄…)
│   ├─ 会话窗口(核心)  —— 按环节推进:AI 提问 → 开口 → 逐词反馈 → 环节记录
│   └─ 会话完成页      —— 鼓励语 + A/B/C/D 总览(简明版)
└─ 家长模式(爸爸)
    ├─ 仪表盘          —— 本周时长/次数/平均分 + 趋势折线
    ├─ 教案库          —— 上传 TXT/DOCX → 解析预览 → 确认入库 → 与 AI 沟通调整
    ├─ 会话记录        —— 列表:日期/课程/各环节评级 → 详情(跟读记录全文)
    ├─ 复习计划        —— 薄弱项 + 第 1/3/7 天回测清单
    └─ 设置            —— API Key、学员信息、发音评估开关、用量
```

---

## 6. 教案体系与解析(基于真实样本)

### 6.1 教案结构(爸爸的格式,系统按此解析)

| 区块 | 说明 | 示例(Unit01) |
|---|---|---|
| 元信息 | 单元号/中文名/英文名/总时长 | Unit 01 · 见面、问候与心情 · Meet, Feel and Find · 25~28 分钟 |
| 关键能力 | 三四年级能力清单 | 自我介绍;How do you feel?;Where is/are...? |
| 带读规则 | 全局规则 | 等 5~7 秒;每轮≤2 句;教师 35%/学员 65%;连续两次未完成 → 示范一次 |
| 短环节 ×3 | 每段:代号/中文名/时长/核心任务 | MEET 见面名片 6 分钟 |
| 环节内部 | 核心词短语 / 语音点 / 关键句型 / 必做对话 / 换词与纠错 / 过关标准 / 转场句 | 见 6.2 |
| 结束检测 | 从每段抽 1 项检测 | MEET 先开口问名字 |
| 记录方式 | A 独立 / B 提示 / C 示范 / D 未完成 | 每段一记录 |
| 结束输出 | 分段评级 + 真实语句 + 薄弱项 + 1/3/7 天回测 + 固定鼓励语 | "You took your time and kept speaking..." |

### 6.2 环节数据结构(DeepSeek 解析输出)

```json
{
  "unit": 1,
  "title_zh": "见面、问候与心情",
  "title_en": "Meet, Feel and Find",
  "segments": [
    {
      "code": "MEET", "name_zh": "见面名片", "minutes": 6,
      "task": "拿一张英文名片,完成两轮交换姓名的对话",
      "words": ["hello", "name", "friend"],
      "phonics": ["hello 的 /h/、name 的 /n/、friend 的 /f/"],
      "patterns": ["What's your name?", "My name is Mike.", "Nice to meet you.", "Nice to meet you, too."],
      "dialogue": [["A", "Hello! I'm Baobao. What's your name?"], ["B", "My name is Mike."], ...],
      "variation": "第二轮换真实姓名,由悦琳先开口",
      "pass_rule": "能说出自己的名字,并完成一次问名字和回应",
      "transition": "Great. We know each other now. Let's check our feelings."
    }
  ],
  "rules": "每次提问后等 5~7 秒;每轮最多两句;教师 35% / 学员 65%",
  "record_scheme": {"A": "独立完成", "B": "提示后完成", "C": "示范后完成", "D": "未完成"}
}
```

### 6.3 跟读记录(会话结束自动生成,替代豆包的手动记录)

| 区块 | 内容 |
|---|---|
| 练习主题 | 课程 + 环节名 |
| 环节评级 | 每段 A/B/C/D + 依据 |
| 真实语句摘录 | 悦琳说出的原句(逐条,含逐词发音数据) |
| 易错词总结 | 如 be 动词缺失、the 省略、book/boat 混淆 |
| 薄弱项 | 只选 2~4 个,进复习计划 |
| 复习建议 | 第 1 / 3 / 7 天短回测清单 |
| 固定鼓励语 | 教案规定的结尾语 |

---

## 7. 设计系统(Apple 风格)

### 7.1 层级与排版
- 字号阶梯:大标题 34 / 章节 28 / 卡片标题 22 / 正文 17 / 说明 13;**学习模式整体放大一档**
- 字重两级:600 标题、400 正文;层级靠字号 + 颜色深浅,不堆字重
- 字色三档:主文字 / 次级文字 / 提示文字

### 7.2 留白(8pt 网格)
- 间距取 8 的倍数:8 / 16 / 24 / 32;页面左右 24~32pt;列表行高 44~48pt
- 留白是层级的一部分:主操作周围留白最大

### 7.3 材质
- 背景纯色(浅灰),无大面积渐变、无装饰阴影
- 毛玻璃仅用于:导航栏、底部工具条、浮动说话按钮
- 卡片圆角 12~16pt、极轻或零阴影,仅用于「选择类」内容(课程卡片)

### 7.4 列表优先
- 会话记录、教案列表 → 列表(行 + 分隔线);卡片只给需要决策的场景
- 检查法:**去掉全部颜色后界面仍能读懂结构 = 颜色用对了**

### 7.5 颜色(只用于交互和状态)
- 主色一个(品牌蓝):主按钮、链接、选中态
- 状态色:正确=绿 / 需改进=琥珀 / 错误=红(仅发音评估与反馈场景)
- 大面积中性色,零装饰配色

### 7.6 交互反馈
| 场景 | 反馈 |
|---|---|
| 按下说话按钮 | 按压 scale 0.98 + 状态变色 |
| 录音中 | 声波/音量可视化,松手自动提交识别 |
| 识别中 | 轻量等待「正在听…」,不阻塞 |
| AI 回复 | 气泡 + 自动朗读,可点击重听 |
| 发音反馈 | 逐词着色(绿/琥珀/红),点击单词播放标准发音 |
| 环节完成 | AI 说"这一段完成"+ 显示 A/B/C/D |
| 错误处理 | 无网/Key 无效 → 明确中文提示 + 解决入口,绝不留白 |

---

## 8. 核心页面布局

### 8.1 学习模式 · 会话页(核心,竖屏优先)

```
┌──────────────────────────┐
│ ← 返回    Unit 01  MEET  │ ← 毛玻璃导航(当前环节)
├──────────────────────────┤
│  AI:「Hello! I'm Baobao. │
│      What's your name?」│ ← 会话流,自动滚动
│  悦琳:「My name is Mike」│
│  逐词: 🟢🟢🟢🟢🟡🟢       │ ← 逐词发音反馈
│  AI:鼓励 + 追问           │
│            ...           │
├──────────────────────────┤
│        ● 按住说话          │ ← 毛玻璃工具条,唯一主操作
└──────────────────────────┘
```

### 8.2 家长模式 · 教案上传(横屏优先)

```
┌──────────────────────────────────┐
│ 教案库                        [＋上传]│ ← 主操作
│ ┌──────────────────────────────┐ │
│ │ 拖入 TXT / DOCX(支持多文件)    │ │
│ └──────────────────────────────┘ │
│ 解析预览(DeepSeek):               │
│   Unit 02 数字、年龄与礼貌借用       │
│   ├ 环节 COUNT 数字侦察 6分钟 ✅   │
│   ├ 环节 AGE  年龄小调查 7分钟 ✅   │
│   └ 环节 BORROW 礼貌借用 8分钟 ✅   │
│ [确认入库]  [让 AI 调整教案]        │
│ 课程列表(列表:Unit/标题/更新时间)   │
└──────────────────────────────────┘
```

### 8.3 家长模式 · 会话记录详情

```
┌──────────────────────────────────┐
│ Unit 01 · 2026-08-06 14:20       │
│ MEET B · FEEL A · FIND C         │ ← 三段评级
│ ── 真实语句摘录 ──                  │
│ "My name is Mike" → 🟢 独立完成    │
│ "I feel worry"   → 🟡 发音/worry/ │
│ ── 易错词总结 ──                    │
│ · worry 词尾 /i/ 读作 /i:/        │
│ ── 复习计划(第 1/3/7 天) ──         │
│ 第 1 天:重练 FIND 的 Where 问句    │
└──────────────────────────────────┘
```

---

## 9. 落地路线

| 阶段 | 内容 | 前置 |
|---|---|---|
| P0 | 双模式框架 + 教案上传解析(TXT/DOCX)+ 课程库 + 学习会话(mock 链路,DeepSeek 先上) | DeepSeek Key |
| P1 | Azure 真实 Key 接入:ASR + TTS + 发音评估逐词反馈 | Azure Key |
| P2 | 跟读记录自动生成 + 会话记录详情 + 复习计划 + 与 AI 调整教案 | — |
| P3 | 局域网部署、用量核对、README、用 Unit01/02 真实教案实测调优 | — |

---

## 10. 账号准备(用户自行处理)

| 服务 | 状态 | 用途 |
|---|---|---|
| DeepSeek 开放平台 | ✅ 已注册(Key 待填入设置页 / `.env`,重启生效) | 教案解析/教师对话/记录/规划 |
| Azure 国际版 | ⏳ 注册中(需非预付外币卡) | 识别/合成/发音评估 |

---

## 11. 技术坑点附录

1. edge-tts 7.x 无 `output_format` 参数 → soundfile≥0.12 直接解码(本地小程序已验证)
2. 国内下 HuggingFace 模型慢 → `HF_ENDPOINT=https://hf-mirror.com`(本项目用云端 Azure,基本不涉及)
3. Azure 国际版注册卡被拒 → 非预付外币卡,账单地址与银行一致,勿用虚拟卡
4. 发音评估 free-form → `PronunciationAssessmentConfig(reference_text="")`
5. DOCX 解析 → python-docx / 直接解包读 document.xml(已验证可提取全文)
6. API Key 只存后端 `.env`,前端零接触
7. 前端 Web Speech 识别需用户手势启动 → 用「开始通话」按钮在手势内 `recognition.start()` 授权,否则 Chrome 拒绝
8. `server.py` 启动须 `_load_env()` 注入 `.env` 到 `os.environ`,否则设置页保存的 Key 不生效(llm.py 读环境变量)

---

## 12. 待确认(默认已按真实教案设定,可调整)

1. 首批课程:直接用现有 **Unit01、Unit02** 两个真实教案作为种子数据
2. 会话模式:默认按教案"三段式"逐环节推进,每段结束记录 A/B/C/D;悦琳可随时喊停
3. 发音反馈:默认 逐词着色 + 点击重听 + 环节评级;鼓励式措辞
4. 家长验证:默认 4 位 PIN
5. 教案上传:默认支持 **TXT / MD / DOCX / DOC / PDF / 图片(PNG·JPG,走 DeepSeek 视觉解析)**;多文件一次上传
6. 复习计划:默认第 1 / 3 / 7 天回测,薄弱项 2~4 个
7. **教学内容**:默认**以每次上传的教案为主**;可混入往期易错点复习(REVIEW 环节,附在教案之后),但**复习内容由家长在「易错点」页勾选确认(≤6 条)**,系统不自动加练
8. **对话交互**:默认**免按键自动通话**——点「开始通话」后,孩子开口即前端 Web Speech 识别(英文)、AI 朗读完自动续听,像豆包通话;无 Key 也能跑(浏览器原生识别+浏览器TTS);Azure Key 到位后发音评估由占位升级为真评估
